<UserControl x:Name="Root"
             x:Class="SonicNextModManager.HorizontalFieldLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="400">
    <Grid>
        <TextBlock Margin="2,0,0,0" Text="{Binding ElementName=Root,Path=Caption}"/>
        <TextBlock HorizontalAlignment="Right" FontStyle="Italic" Foreground="Gray" Margin="0,0,2,0" Text="{Binding ElementName=Root, Path=Description}"/>
    </Grid>
</UserControl>
